import os
def interaction_model_linear(sub1, sub2, in_dir, out_dir, lambda_high, lambda_low):
	# seperate dataset by 3 parts
	# determine lambda value
		# load lambda
		# load data part A to find parameters
		# test with part B to get performance of lambda
		# record the performance, and increase lambda
	# once the lambda value is determined, test on part C
	# save part C performance and the output

